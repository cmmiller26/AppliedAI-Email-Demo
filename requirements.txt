# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0

# HTTP client for Graph API
httpx==0.25.1

# Authentication
msal==1.25.0

# Azure AI Foundry / OpenAI
# Note: The openai SDK works with both direct Azure OpenAI Service
# and Azure AI Foundry (no code changes needed).
# AI Foundry provides the same API endpoints with enhanced monitoring,
# evaluation tools, and access to Prompt Flow.
openai==1.3.5

# Optional: For native AI Foundry SDK features (future use in Phase 7-8)
# Uncomment these if you want to use Prompt Flow, evaluation APIs, or
# other AI Foundry-specific features beyond basic OpenAI calls.
# azure-ai-inference>=1.0.0
# azure-identity>=1.14.0

# Environment variables
python-dotenv==1.0.0

# Date/time parsing
python-dateutil==2.8.2

# Background task scheduling
APScheduler==3.10.4

# HTML templates (for dashboard)
jinja2==3.1.2

# Optional: for better async support
aiofiles==23.2.1