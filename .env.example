# =============================================================================
# Azure App Registration (OAuth Authentication)
# =============================================================================
# App Registration: app-appliedai-classifier-poc
# Located in: Azure Portal → Azure Active Directory → App registrations
#
# Setup Instructions:
# 1. Copy Application (client) ID → CLIENT_ID
# 2. Copy Directory (tenant) ID → TENANT_ID
# 3. Create client secret with description "poc-local-dev" → CLIENT_SECRET
# 4. Set redirect URI: http://localhost:8000/auth/callback
# 5. Add API permissions: Mail.Read, Mail.ReadWrite, offline_access
#
# ⚠️ SECURITY NOTES:
# - Copy the client secret VALUE immediately (you cannot view it again!)
# - Client secret description: "poc-local-dev"
# - Expires: 24 months (set calendar reminder to rotate)
# - For production: use Azure Key Vault instead of .env files
# =============================================================================

CLIENT_ID=your_application_client_id_here
TENANT_ID=your_directory_tenant_id_here
CLIENT_SECRET=your_client_secret_value_here

# OAuth redirect URI - must match App Registration configuration
REDIRECT_URI=http://localhost:8000/auth/callback

# =============================================================================
# Azure AI Foundry Configuration
# =============================================================================
# This project uses Azure AI Foundry (formerly Azure AI Studio) as the
# centralized platform for AI operations. Azure AI Foundry provides enhanced
# monitoring, evaluation tools, and access to Prompt Flow for complex workflows.
#
# Resource Hierarchy:
# - Hub: aih-appliedai-classifier-poc (North Central US)
# - Project: aip-appliedai-classifier-poc
# - Model Deployment: gpt-4o-mini
#
# The existing OpenAI Python SDK works seamlessly with AI Foundry - no code
# changes needed! The endpoint below connects through the AI Foundry infrastructure.
#
# Benefits of AI Foundry:
# - Built-in monitoring and cost tracking
# - Evaluation datasets for testing accuracy
# - Prompt Flow for visual workflow design
# - Content safety and moderation tools
# - A/B testing capabilities
# =============================================================================

# Azure OpenAI Service (connected to AI Foundry)
AZURE_OPENAI_KEY=your_azure_openai_api_key_here
AZURE_OPENAI_ENDPOINT=https://aih-appliedai-classifier-poc.cognitiveservices.azure.com/
AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Optional: Azure AI Foundry connection string (for advanced features)
# AZURE_AI_PROJECT_CONNECTION_STRING=your_connection_string_here

# =============================================================================
# SECURITY NOTES
# =============================================================================
# ⚠️ NEVER commit this file to git!
# ⚠️ Copy the client secret value immediately when created (can't view again)
# ⚠️ Set calendar reminder to rotate client secret before expiration (24 months)
# ⚠️ For production: use Azure Key Vault instead of .env files
# ⚠️ Keep file permissions restrictive: chmod 600 .env
#
# Azure Resources:
# - App Registration: app-appliedai-classifier-poc
# - AI Foundry Hub: aih-appliedai-classifier-poc
# - AI Foundry Project: aip-appliedai-classifier-poc
# =============================================================================